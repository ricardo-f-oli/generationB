package com.generationb.reporting;

/** Requirement #51: the three report types the team actually produces. */
public enum ReportType {
    MONTHLY_SEEDING,
    CAMPAIGN_WRAP,
    MAILER_CONVERSION
}
